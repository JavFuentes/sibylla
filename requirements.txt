# Sibylla — dependencias del ingestor (prototipo)
feedparser>=6.0.12        # RSS/Atom (arXiv, Google News, medios)
requests>=2.32.0          # HTTP para APIs (PubMed, Hacker News, X)
PyYAML>=6.0.2             # cargar config/sources.yaml
python-dotenv>=1.0.1      # cargar claves desde .env
python-dateutil>=2.9.0    # parseo robusto de fechas
Jinja2>=3.1.0             # plantillas de la web estática (sibylla/web.py)
tzdata>=2025.1            # zonas horarias para zoneinfo (Windows/CI no traen base del sistema)
trafilatura>=1.12.0       # extracción del cuerpo de artículos de prensa (resúmenes con LLM)
